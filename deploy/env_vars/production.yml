---

# Git settings.
setup_git_repo: yes
git_branch: master

# Database settings.
db_user: "{{ application_name }}"
db_name: "{{ application_name }}"
db_password: a$ider$c3ntr@l


# Gunicorn settings. For the number of workers, a good rule to follow is
# 2 x number of CPUs + 1
gunicorn_num_workers: 3

# Setting this to 1 will restart the Gunicorn process each time
# you make a request, basically reloading the code. Very handy
# when developing. Set to 0 for unlimited requests (default).
gunicorn_max_requests: 0

# Application settings.
django_settings_file: "{{ application_name }}.settings.production"
django_secret_key: "z7uf-%fvf5ipid%$$hgsn!f&x=3vk-7#*c%bluzn$gx8igsi%t"


# RabbitMQ settings.
rabbitmq_server_name: "{{ inventory_hostname }}"

rabbitmq_admin_user: ticket
rabbitmq_admin_password: a$ider$c3ntr@l

rabbitmq_application_vhost: "{{ application_name }}"
rabbitmq_application_user: "{{ application_name }}"
rabbitmq_application_password: a$ider$c3ntr@l


# Celery settings.
celery_num_workers: 2
flower_admin_password: a$ider$c3ntr@l

broker_url: "amqp://{{ rabbitmq_application_user }}:{{ rabbitmq_application_password }}@localhost/{{ rabbitmq_application_vhost }}"

run_django_db_migrations: yes
run_django_collectstatic: yes
